# Copy values into User Secrets or environment variables (do not commit real secrets).

ConnectionStrings__DefaultConnection=Server=.;Database=TaskManagementDb;Trusted_Connection=True;TrustServerCertificate=True
JwtSettings__Key=CHANGE_ME_AT_LEAST_32_CHARACTERS_LONG
JwtSettings__Issuer=TaskManagementApp
JwtSettings__Audience=TaskManagementUsers
